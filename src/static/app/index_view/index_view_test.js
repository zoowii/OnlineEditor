'use strict';

describe('acg.index_view module', function () {

    beforeEach(module('acg.index_view'));

    describe('index_view controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var indexView1Ctrl = $controller('IndexView1Ctrl');
            expect(indexView1Ctrl).toBeDefined();
        }));

    });
});